{{- if and .Values.server.enabled .Values.server.migrate.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "fynncloud.fullname" . }}-server-migrate
  labels:
    {{- include "fynncloud.labels" . | nindent 4 }}
    app.kubernetes.io/component: server-migrate
  annotations:
    {{- with .Values.server.migrate.podAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  template:
    metadata:
      labels:
        {{- include "fynncloud.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: server-migrate
    spec:
      restartPolicy: OnFailure
      serviceAccountName: {{ include "fynncloud.serviceAccountName" . }}
      {{- include "fynncloud.imagePullSecrets" . | nindent 6 }}
      {{- with .Values.server.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-postgresql
          image: {{ include "fynncloud.busyboxImage" . | quote }}
          command:
            - sh
            - -c
            - |
              until nc -z {{ include "fynncloud.postgresql.host" . }} {{ include "fynncloud.postgresql.port" . }}; do
                echo "Waiting for PostgreSQL at {{ include "fynncloud.postgresql.host" . }}:{{ include "fynncloud.postgresql.port" . }}..."
                sleep 2
              done
              echo "PostgreSQL is ready"
      containers:
        - name: migrate
          image: "{{ .Values.server.image.repository }}:{{ .Values.server.image.tag }}"
          imagePullPolicy: {{ .Values.server.image.pullPolicy }}
          {{- with .Values.server.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          command:
            {{- toYaml .Values.server.migrate.command | nindent 12 }}
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "fynncloud.postgresql.secretName" . }}
                  key: {{ include "fynncloud.postgresql.secretKey" . }}
            - name: DATABASE_URL
              value: "postgresql://{{ include "fynncloud.postgresql.username" . }}:$(POSTGRES_PASSWORD)@{{ include "fynncloud.postgresql.host" . }}:{{ include "fynncloud.postgresql.port" . }}/{{ include "fynncloud.postgresql.database" . }}?sslmode={{ include "fynncloud.postgresql.sslMode" . }}"
            - name: STORAGE_PATH
              value: "/app/Storage/"
            - name: APP_NAME
              value: {{ .Values.server.env.APP_NAME | quote }}
            - name: PRIMARY_COLOR
              value: {{ .Values.server.env.PRIMARY_COLOR | quote }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "fynncloud.jwt.secretName" . }}
                  key: jwt-secret
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "fynncloud.jwt.secretName" . }}
                  key: encryption-key
            - name: CORS_ALLOWED_ORIGINS
              value: {{ .Values.server.env.CORS_ALLOWED_ORIGINS | quote }}
            - name: FRONTEND_URL
              value: {{ .Values.server.env.FRONTEND_URL | quote }}
            - name: MAX_CHUNK_SIZE
              value: "{{ .Values.server.env.MAX_CHUNK_SIZE }}mb"
            - name: MAX_BODY_SIZE
              value: "{{ .Values.server.env.MAX_BODY_SIZE }}mb"
            {{- with .Values.server.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: storage
              mountPath: /app/Storage
            {{- with .Values.server.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      {{- with .Values.server.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.server.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.server.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: storage
          emptyDir: {}
        {{- with .Values.server.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
{{- end }}
